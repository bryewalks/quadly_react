import React from 'react'

import { ControlsContainer } from './style'

export const MapControls = ({children, showing}) => {

  return (
    <ControlsContainer showing={ showing }>
      
    </ControlsContainer>
  )
}

export default MapControls